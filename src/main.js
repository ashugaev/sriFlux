import './index.pug'
import './index.scss'

// window.addEventListener('load', ()=> {
//     let titleBox, titleIn
//     const titles = document.querySelectorAll(".eventItem__title")
//     titles.forEach((item, i) => {
//         titleBox = item.offsetHeight;
//         titleIn = item.querySelector('span').offsetHeight
//         const titleText = item.querySelector('span').textContent
//         console.log(titleBox, titleIn)
//         if(titleBox < titleIn) item.querySelector('span').textContent += '...'
//         console.log(titleText.split('\n').length)
//     })
// })